#!/usr/bin/env bash

set -e

[ ! -d "${HOME}/media/screenshots" ] && mkdir -p "${HOME}/media/screenshots"

f="${HOME}/media/screenshots/$(date +%s).png"
maim -s -m 1 "${f}"
notify-send "Saved screenshot to ${f}"
